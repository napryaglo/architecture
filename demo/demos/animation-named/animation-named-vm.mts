// AnimationNamedVM — empty marker model. This demo is markup-only;
// the VM exists solely so the platform's ContentControl can auto-
// resolve the template by data type.
import { MuralBase } from '@pragmatic-lab/mural/runtime';

export class AnimationNamedVM extends MuralBase { }
